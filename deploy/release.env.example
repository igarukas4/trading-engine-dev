# Copy to a protected path on the VPS and pin BACKEND_IMAGE to a registry digest.
DOMAIN=dashboard.example.com
ACME_EMAIL=ops@example.com
CADDY_BASIC_AUTH_USER=operator
BACKEND_IMAGE=ghcr.io/example/trading-engine@sha256:replace-with-a-real-image-digest
